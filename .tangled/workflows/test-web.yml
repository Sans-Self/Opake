image: oven/bun:1-debian
engine: "kubernetes"
architecture: arm64

when:
  - event: ["push", "pull_request"]
    branch: ["main"]

steps:
  - name: "Install deps"
    command: |
      cd web
      bun install --frozen-lockfile

  - name: "Test"
    command: |
      cd web
      bun test
