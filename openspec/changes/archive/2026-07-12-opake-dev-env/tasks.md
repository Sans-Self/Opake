# Tasks: opake-dev-env

## 1. Spikes (retire the design risks first)

- [x] 1.1 Spike: log a fixture actor in via the web UI against any PDS, snapshot `storageState({ indexedDB: true })`, restore in a fresh context, verify the WASM session restores across reload (D4 go/no-go; on failure, document and pivot to the dev-build login hook) — GO: full OAuth flow driven headlessly (PDS login + consent pages scriptable), 1 IndexedDB db in snapshot, restored context authenticated, survives reload; anonymous control bounces to login (`tests/spikes/storage-state-spike.ts`)
- [x] 1.2 Spike: minimal compose of plc + one PDS + relay + jetstream — pick the relay implementation/tag, settle the handle scheme, prove `requestCrawl` → jetstream frames flow (resolves both D2 open questions) — GO, real frame captured for a local did:plc (`dev-env/spike/NOTES.md` + `captured-frame.json`); stock indigo relay unmodified via Caddy TLS on :443 + local dev CA + public-range docker subnet (relay's SSRF guard rejects private IPs and non-443 ports, no flag); handle scheme `pds.test`; jetstream published image is v0.1.0 (`--ws-url`, 15s idle self-kill → restart policy)
- [x] 1.3 Spike: OAuth against the dev-env PDS — loopback client_id from the web app, drive the authorization/consent pages headlessly, and settle how `at.opake.authFullAccess` permission-set resolution works under the blockade (local NSID authority fixture vs PDS config) — GO (`dev-env/spike/oauth-probe.ts`, NOTES.md "OAuth"): loopback client_id served end-to-end, granular scopes granted verbatim + real write authorized, permission-set question DISSOLVED (`oauth_scope()` enumerates, no `include:` — no NSID authority needed until include: lands); real gap is handle resolution (`resolve_pds_for_login` external fallback) → well-known via Caddy or login-by-DID, decided in web tier

## 2. Compose stack and bootstrap

- [x] 2.1 `dev-env/docker-compose.yml`: plc, pds-a/b/c (each behind a Caddy TLS vhost on :443, one shared local dev CA, relay trusts via `SSL_CERT_FILE`), relay (local Dockerfile from spike — git-context build impossible), jetstream (build current from source to kill the v0.1.0 idle self-kill), postgres, indexer — pinned tags, healthchecks, public-range internal subnet (SSRF-guard requirement; MUST land together with the egress blockade) + host-access network for published ports
- [x] 2.2 Bootstrap script: invite codes → accounts from fixture mnemonics → identity derivation → publish `at.opake.publicKey/self` per actor → `requestCrawl` per PDS
- [x] 2.3 Fixture definitions: named actors with fixed mnemonics distributed across pds-a/b/c, clearly labeled as public test vectors (no DIDs hardcoded anywhere) — `dev-env/fixtures/actors.json` (6 actors, 2 per PDS, handles `{name}.{pds}.test`) + `generate_mnemonics.py` (provenance; uses the crate's own wordlist); all phrases validated against `opake_crypto::parse_mnemonic`
- [x] 2.4 Justfile recipes `dev-env-up` (blocks on readiness gate), `dev-env-down`, `dev-env-reset` (reset relay/PDS state and jetstream data-dir together — a stale jetstream cursor stalls the stream); indexer wired via `JETSTREAM_URL`
- [x] 2.5 Verify hermeticity: full pipeline (create record → indexed) succeeds under the egress blockade; PDS outbound config (SMTP, appview/mod-service URLs) neutralized or pointed inward — PASS (all 4 dev-env spec acceptance scenarios; `dev-env/README.md`)

## 3. Client configuration for local resolution

- [x] 3.1 Thread the DID resolver base URL through runtime config in opake-core so the web/WASM client can target the local PLC (`OPAKE_PLC_DIRECTORY` stays as the native override; the constant becomes the config default) — `set_plc_directory_url` (OnceLock, set-once; override > env > default, precedence unit-tested) + `setPlcDirectoryUrl` wasm export, typings regenerated; 574/574 core green; surfaced as static `Opake.setPlcDirectoryUrl` in the SDK (set-once semantics documented) after group-4 work found the export unreachable from apps/web
- [x] 3.2 Indexer PLC base-URL config: new config key with `https://plc.directory` default, consumed by `auth/key_fetcher.ex` and `backfill.ex`; verify a fixture actor's SSE token exchange succeeds against the dev-env (regression: existing auth tests stay green on the default) — CODE DONE (`plc_directory_url` config + `PLC_DIRECTORY_URL` env override, both call sites converted, 86/86 tests green); VERIFIED against the stack (alice authenticated via local PLC, exact 32-byte key resolved)
- [x] 3.3 Web app dev configuration for dev-env mode: PDS/indexer/resolver URLs, indexer CORS or proxy settled (design open question), and the handle-resolution choice (Caddy-served `/.well-known/atproto-did` per handle vs login-by-DID — `resolve_pds_for_login`'s external fallback never fires under the blockade) — DONE: `.env.devenv`, `Opake.setPlcDirectoryUrl` seeded at boot + pre-login, Caddy wildcard per-handle well-known (handle login works), indexer CORS set
- [x] 3.4 Verify CLI against dev-env: login, upload, download against pds-a with `OPAKE_PLC_DIRECTORY` set — `dev-env/bootstrap/verify-cli.sh` PASS: createSession + recover + upload + indexed roundtrip (~4s through the pipeline) + cross-PDS pubkey fetch by DID over the dev-CA TLS path (`SSL_CERT_FILE`); handle→DID for foreign actors remains the known gap (by-DID sidesteps)

## 4. Web e2e harness

- [x] 4.1 Playwright into the `tests/` workspace: config with setup project + per-actor storageState artifacts, workers partitioned by actor (D5), and global route interception failing any non-localhost request (browser-side blockade — the browser runs outside the compose network) — DONE; the blockade caught two real app escapes on day one (bsky PUBLIC_API profile fetch, localhost-vs-127.0.0.1 OAuth origin)
- [x] 4.2 Setup project: full OAuth flow per fixture actor (handle → PDS authz/consent pages → callback → mnemonic import), persist storage states; SDK-driven session mint (`loginWithAppPassword` in page context) as documented fallback; move the actor password from bootstrap's `ACTOR_PASSWORD` env into `actors.json` when this lands (the harness needs it per actor) — GREEN for all 6 actors via the REAL OAuth flow (fallback not needed); passwords now per-actor in fixtures; web served at 127.0.0.1:5199 (RFC 8252: PDS rejects `localhost` as loopback redirect host)
- [x] 4.3 First specs: session-restore-across-reload, workspace create/rename/delete, document upload/download — each citing the openspec scenario it exercises (D6 format) — GREEN (session-restore, workspace-lifecycle, document-roundtrip specs); full suite 15/15 zero-retry 2026-07-12 after two harness fixes: `.env.devenv` lifted into the Playwright webServer env (ambient `.envrc` VITE_INDEXER_URL shadowed the mode file — vite gives process env precedence) and a save-retry in the rename spec around a real product bug (fresh-workspace mutations 403 "not a member" at the creator until the genesis keyring is indexed; no client retry — findings queue)
- [x] 4.4 Cross-PDS spec: manager on pds-a adds member on pds-b, membership visible both sides — citing workspace-membership scenarios (`membership-cross-pds.spec.ts`; share-accept flow folded into membership since invitations are lexicon-only today)
- [x] 4.5 Dedicated OAuth spec for flow-specific behavior beyond the setup happy path: callback error paths, pending-login expiry (10-min TTL), cancelled consent — GREEN (`auth-flows.spec.ts`, all citing wasm-security-boundary requirements)

## 5. CLI federation tier

- [x] 5.1 Dev-env mode in the vitest harness: env-selected PDS resolution in `helpers/pds.ts`, `tests/tests/federation/` suite only runs in that mode; fake-pds default path untouched — `testEnv()`/`devenvActors()` added to `tests/helpers/pds.ts` (fake path byte-unchanged); `vitest.config.ts` excludes `tests/federation/**` unless `OPAKE_TEST_ENV=devenv`, plus a `describe.skipIf` guard; default `vitest run` is byte-identical to baseline (federation not collected). CLI driven inside the compose network (`tests/helpers/devenv.ts` + `devenv-cli.sh`) because only Caddy is host-published and the host doesn't resolve `*.test`
- [x] 5.2 Hermetic leave smoke test: two actors on different PDSes, shared workspace, leave, both sides' views verified via indexer (absorbs the queued live-account smoke test and keyring-delete-outcomes task 5.2: genesis-delete sidebar-survives + sole-record drop) — `tests/tests/federation/leave-smoke.test.ts`, 3/3 green via `just e2e-federation` (alice@pds-a + carol@pds-b, foreign member addressed by DID): cross-PDS leave drops the leaver from both indexer views; genesis-delete on a superseded chain keeps the workspace live (`unchanged`); sole-record delete tears it down (`torn_down`). Assertions poll the indexer-backed `workspace ls`; keyring deletes driven over XRPC deleteRecord with the actor's own session

## 6. Traceability lint

- [x] 6.1 Extend `scripts/spec_lint.py`: parse spec citations from e2e test files, resolve capability/requirement pairs against `openspec/specs/`, fail on dangling citations; keep `just spec-lint` green — `spec:<capability> § <requirement>` in `tests/**/*.ts` resolved against `### Requirement:` headings; probe-tested both failure modes (unknown capability, unknown requirement) and clean pass

## 7. Documentation and closure

- [x] 7.1 `dev-env/README.md`: topology diagram, lifecycle, fixture actor table, how to add an actor/PDS — DONE: topology + network split (SSRF-guard subnet rationale), reset blast radius (E2E_REAUTH), image provenance table, bootstrap rationale (seed-then-recover, cabinet-root seeding), add-actor/add-PDS runbooks, consumer map incl. the vite env-precedence gotcha, known gaps (host-side handle resolution, no delete primitive)
- [x] 7.2 Wire e2e entrypoints into the justfile (`just e2e-web`, `just e2e-federation`); document which tiers run where (default validate vs dev-env-gated) — both recipes added under an E2E header documenting the split (validate stays hermetic-without-docker; these two are dev-env-gated); a `_dev-env-check` preflight curls `https://indexer.test/api/health` through Caddy and fails fast with "run 'just dev-env-up' first" when down. `just e2e-web` → `bunx playwright test --project=e2e` (9 passed); `just e2e-federation` → `OPAKE_TEST_ENV=devenv bunx vitest run tests/federation/` (3 passed)
- [x] 7.3 Sync delta specs to `openspec/specs/` and archive the change — both delta capabilities (`dev-env`, `e2e-testing`) materialized into canon as new specs (pure ADDED); spec-lint 9 specs / 35 citations / 0 dangling; `openspec validate` green; archived alongside keyring-delete-outcomes (whose deferred 5.2 smoke landed in this change's federation tier)
