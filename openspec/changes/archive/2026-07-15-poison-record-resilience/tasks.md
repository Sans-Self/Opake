# Tasks: poison-record-resilience

## 1. Version contract, vocabulary pin & lexicon audit (D5, D8, D9)

- [x] 1.1 Verify every record type (`Directory`, `Document`, `Keyring`, `Grant`, `PublicKey`, `AccountConfig`, `PairRequest`, `PairResponse`) carries top-level `opakeVersion`; add a unit test enumerating ALL record types asserting the field is present and top-level on all serialized forms (cite: record-validity § opakeVersion is a stable protocol contract)
- [x] 1.2 Remove `#[serde(default = "default_version")]` masking from ALL record types (verified on grant/publicKey/accountConfig/pairResponse; confirm directory/document/keyring too): missing `opakeVersion` (or non-integer) is corrupt, not defaulted to current; reconcile with `list_collection_raw`'s skip behavior so both paths classify identically (cite: record-validity § opakeVersion is a stable protocol contract, scenario "missing version is corrupt")
- [x] 1.3 Create the version-vocabulary table as a language-neutral JSON artifact (lexicons/vocabulary.json; cumulative per version; covering the closed vocabulary-bearing field list: key-wrap algo identifiers, content-encryption algo identifiers, publicKey algo identifiers, pairing KEM/cipher algo identifiers, keyring member roles — NOT union $type tags); opake-core loads it at compile time; classification helper: known-version vocabulary violation ⇒ corrupt; version-peek + known-schema required-field floor precede typed parse everywhere classification happens (floor missing ⇒ corrupt regardless of declared version) (cite: record-validity § schema evolution is additive and vocabulary is version-pinned; § version is peeked before structural judgment)
- [x] 1.4 Open-string wire parsing for ALL vocabulary-bearing values: algo identifier fields and the `keyringMember.role` enum (currently a closed serde enum that hard-fails unknown roles) parse unknown identifiers instead of failing serde; understanding is checked against the vocabulary table, not the type system; replace the compile-time `EXPECTED_X25519_ALGO`/`EXPECTED_ML_KEM_ALGO` constants in resolve.rs with vocabulary-table lookups (cite: record-validity § schema evolution is additive and vocabulary is version-pinned)
- [x] 1.5 Pre-v1 lexicon audit: every field in every `lexicons/*.json` reviewed for required-vs-optional with the additive-forever consequence in mind; all crypto-envelope fields confirmed required; audit outcome recorded in the change (cite: record-validity § PDS lexicon validation is the first, untrusted layer)
- [x] 1.6 Document the evolution rules — ignore-safe field additions, version-pinned vocabulary, new-NSID escape hatch, the `_lexicon` publication as layer 0 — in lexicons/README.md, citing the additive-evolution requirement

## 2. Lenient read paths (D1, D2)

- [x] 2.1 Corrupt-reference type (`{uri: Option<String>, reason}`) and lenient per-record envelope deserialization in `crates/opake-core/src/indexer/types.rs`: version peek on the raw value FIRST; future version + known-schema required-field floor present ⇒ kept + marked needs-newer-client (no full typed parse); future version + floor missing ⇒ corrupt reference (laundering guard); known versions: parse failure / missing version / vocabulary violation ⇒ corrupt reference (warn, skip) (cite: record-validity § corrupt records are skipped per-record, never wholesale; § future-version records are visible, locked, and actionable)
- [x] 2.2 Apply to `TreeDelta.directories`, `TreeDelta.documents`, `WorkspacesResponse.workspaces`, `InboxResponse.grants`; surface corrupt references and needs-newer marks on each response type
- [x] 2.3 `list_collection` gains a per-caller policy parameter; user-facing callers (grants `sharing/list.rs`, keyrings `keyrings/list.rs`, CLI cabinet tree `directories/tree.rs`, pending shares `sharing/pending.rs`) adopt the counted policy (corrupt skipped+referenced, future-version kept+marked — result entry types such as `GrantEntry` grow the needs-newer mark so downstream logic can read it); recipient-key resolution (`resolve.rs` singleton getRecord path — NOT the caller-less `list_collection_public`) gains error variants distinct from `RecipientNotReady`: corrupt-key and newer-client-required each refuse the share with their reason, while no-key-at-all keeps existing not-ready warn/queue semantics; pairing callers (`pairing/cleanup.rs` both sites) explicitly retain existing skip semantics (cite: record-validity § PDS collection listing policy is per-caller, scenario "unreadable public key blocks sharing with a reason")
- [x] 2.4 Re-verify each converted caller against downstream `check_version` sites (documents/download, directories/entries, manager/tree, metadata/read, resolve, member ops): future-version records reaching them must degrade per-record, not error the operation
- [x] 2.5 Regression test `bug__malformed_directory_bricks_snapshot`: malformed `at.opake.directory` (missing `keyWrapping`/`encryptedMetadata`) alongside well-formed records — well-formed all parse, corrupt reference carries the URI (cite: record-validity § corrupt records are skipped per-record, never wholesale)
- [x] 2.6 Future-version fixture test: well-formed record with `opakeVersion = SCHEMA_VERSION + 1` stays visible, marked; vocabulary-violation fixture (v1 record, unknown algo) classifies corrupt (cite: record-validity § schema evolution is additive and vocabulary is version-pinned, scenario "undeclared vocabulary is corrupt")

## 3. Degradation semantics & SSE parity (D3, D10)

- [x] 3.1 TreeKeeper: placeholder node for addressable corrupt containers (client-assigned name, children attached beneath; position from surviving references), fed by corrupt references from 2.1; count-only when no URI; authorized-snapshot invariant — no placeholder for unreferenced elements (cite: record-validity § corrupt containers render as placeholders)
- [x] 3.2 WorkspaceKeeper: distinct skipped-workspace signal in listing state (cite: record-validity § corrupt workspaces are skipped with a distinct signal)
- [x] 3.3 SSE per-event lenience — three concrete changes: (a) `from_name_and_data` parses the envelope with a raw payload first so the URI survives a corrupt inner record; (b) the event type gains a corrupt-record variant carrying `{uri, reason, scope}` so keepers can placeholder/signal it; (c) the parse-vs-transport split lands where the information exists — `from_name_and_data` returns Ok(corrupt-event), never Err, for a record-level failure; both connection `next_event` impls (wasm + reqwest) and the consumer then treat only transport errors as reconnectable (today a corrupt record causes a reconnect loop). Same keeper state as snapshot delivery; applies to tree, workspace and inbox keepers. Native CLI daemon inherits the fix through shared events.rs/connection code — its handler safely ignores the new variant; verify, don't assume (cite: record-validity § SSE delivery matches snapshot delivery)
- [x] 3.4 Propagate corrupt references, needs-newer marks and placeholder state through opake-wasm/SDK bindings; minimal surfacing: web badge on the tree view, CLI `-v` line
- [x] 3.5 Keeper tests: snapshot/SSE convergence on the same corrupt record; placeholder position stability across snapshot refresh and SSE patch (cite: record-validity § SSE delivery matches snapshot delivery, scenario "corrupt upsert converges with snapshot handling")

## 4. Write strictness & crypto parameters (D1, D4, D5, D11)

- [x] 4.1 Chain walk: corrupt link ⇒ reject proposed head, retain last verifiable state and surface the degradation in `directories/chain.rs` mirror-validation (cite: record-validity § writes refuse state they do not fully understand; tree-chains § unverifiable heads degrade to the last verifiable state)
- [x] 4.2 Mutation guards: chain advance and keyring re-wrap refuse on corrupt links (error names the link) and future-version links (error names the link and states a newer client is required) (cite: record-validity § future-version records are visible, locked, and actionable, scenario "future-version link blocks the write with an actionable message")
- [x] 4.3 Heal guard: `heal_stale_grants` never revokes a grant it cannot fully understand — corrupt and future-version grants are left untouched and reported (cite: record-validity § writes refuse state they do not fully understand, scenario "healing never revokes what it cannot read")
- [x] 4.4 Thread the record's declared `opakeVersion` AND declared algo into the unwrap path: signature change on `unwrap_key`/`hkdf_info` and every unwrap call site (the version lives on the enclosing record, not `WrappedKey` — audit all sites so none silently keeps the compile-time `SCHEMA_VERSION` or `HYBRID_WRAP_ALGO` constants); transcript binding documented as self-consistency, not authentication; test asserts the mechanism with a synthetic future version (mechanism-only until a real v2 exists — see design risks) (cite: record-validity § cryptographic parameters derive from the record's declaration)
- [x] 4.5 Tests: refused advance issues no PDS write; unverifiable head falls back; future-version link blocks mutation while view stays intact

## 5. Indexer ingest gate (D6 — phase 2, may land post-flip)

- [x] 5.1 Minimal lexicon validator over `at.opake.*` lexicons in the indexer, driven by `lexicons/*.json` as source of truth, plus the shared vocabulary artifact (lexicons/vocabulary.json) for known versions; cross-language conformance test pinning Rust and Elixir to identical vocabulary interpretation
- [x] 5.2 Validate every incoming `at.opake.*` record at ingest: structural check for all versions, vocabulary check for known versions; malformed or vocabulary-violating ⇒ not indexed (rejection, not deletion) (cite: record-validity § indexer validates structure for all versions and vocabulary for known versions)
- [x] 5.3 ExUnit coverage: refused record absent from snapshot and stream; well-formed future-version record present; claimed-future malformed record refused; vocabulary-violating known-version record refused; per-file forwarded-for IPs in conn-driving tests (Hammer)

## 6. Verification & flip gate

- [x] 6.1 `just rust-test` + `just validate` green; spec-lint citations resolve
- [x] 6.2 Reproduce the original incident against dev-env (malformed directory write) and verify everything-but-the-bad-record renders with visible corrupt reference
- [ ] 6.3 Phase 1 (groups 1-4, 6) complete ⇒ #23 closable pending approval; unblocks Public flip milestone

## 7. Tracked follow-ups (data landed; presentation/refinement outstanding)

- [ ] 7.1 CLI `-v` line surfacing corrupt/needs-newer counts on `opake tree`/list (data already exposed via bindings; presentation lives in apps/cli/commands/tree.rs, gated on the manager cabinet path)
- [ ] 7.2 Delta-sync (poll) path + placeholder persistence: `try_sync_deltas` accumulates `delta.unreadable` and applies it to the cached-path tree; decide whether unreadable refs persist across a cold cache reload (currently re-derived on next fetch/SSE). Cold-start bootstrap path is wired (manager/tree.rs `bootstrap_tree`); this extends it to the incremental path.
- [ ] 7.3 Error-taxonomy tidy (optional): resolve.rs uses `Error::InvalidRecord` with distinct messages for corrupt-key vs newer-client-required; the chain lane added `ChainLinkCorrupt`/`ChainLinkNeedsNewerClient` (chain-scoped). If programmatic distinction is wanted at the public-key layer, introduce shared reason-carrying variants rather than reusing the chain-scoped ones. Functional today; cosmetic.
