// Placeholder — heal_stale_grants tests will be added when the re-wrapping
// logic is fully implemented. For now, the function only handles the delete
// path (recipient gone) which is tested via the e2e tests.
