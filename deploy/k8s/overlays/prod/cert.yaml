apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opake-at
spec:
  secretName: opake-at-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - opake.at
    - indexer.opake.at
